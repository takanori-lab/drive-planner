export const ADMIN_PAGE = `<!doctype html>
<html lang="ja"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width">
<title>AIログをエクスポート</title><style>body{font-family:system-ui;max-width:34rem;margin:4rem auto;padding:1rem}label,input,button{display:block;width:100%;box-sizing:border-box;margin:.75rem 0}input,button{padding:.75rem}#error{color:#b00020}</style></head>
<body><h1>AIログをエクスポート</h1><form autocomplete="off"><label for="key">AI_LOG_EXPORT_KEY</label>
<input id="key" type="password" autocomplete="new-password" required><button type="submit">JSONLをダウンロード</button><p id="error" role="alert"></p></form>
<script>document.querySelector('form').addEventListener('submit',async(e)=>{e.preventDefault();const key=document.querySelector('#key');const error=document.querySelector('#error');error.textContent='';try{const response=await fetch('/v1/admin/ai-logs/export',{headers:{Authorization:'Bearer '+key.value},cache:'no-store'});if(!response.ok)throw new Error('ダウンロードに失敗しました。キーを確認してください。');const blob=await response.blob();const disposition=response.headers.get('Content-Disposition')||'';const match=disposition.match(/filename="([^"]+)"/);const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download=match?match[1]:'drive-planner-ai-logs.jsonl';a.click();URL.revokeObjectURL(url)}catch(ex){error.textContent=ex.message}finally{key.value=''}});</script></body></html>`;
